[project]
name = "investment-tracker"
version = "0.1.0"
description = "TUI Investment Tracker App built with Python and Textual"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "textual>=7.5.0",
]
